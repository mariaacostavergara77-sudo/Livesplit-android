name: livesplit_audio
description: "Un auto-splitter flotante para Android."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_audio_capture: ^1.1.2
  fftea: ^1.2.0
  permission_handler: ^11.3.1
  flutter_overlay_window: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
